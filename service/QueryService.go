package service

// ini untuk proses query
func GetAll(Model interface{}) (interface{}, error) {
	// db := config.GetDatabaseConnection()
	// defer db.Close()
	//var model []Model
	//db.Find(&model)
	//return model
	var Modelel []Model
	return model
}
